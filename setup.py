# Shared library for utulek Python code.
import setuptools

setuptools.setup(
    name="persona_convergence",
    packages=["persona_convergence"],

    install_requires=["numpy", "matplotlib", "scikit-learn"]
)
